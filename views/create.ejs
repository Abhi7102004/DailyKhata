<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Hisaab</title>
    <link rel="stylesheet" href="/app.css">
    <style>
        .fade-in {
            animation: fadeIn 1s ease-in-out;
        }
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
    </style>
</head>
<body class="bg-gray-100">
    <%- include('partials/header') %>
    <div class="px-20 py-5 fade-in">
        <h2 class="text-lg font-bold mb-4 text-center my-4">Create New Hisaab</h2>
        <form action="/create/<%= fileName %>" method="post" class="space-y-4 w-6/12 mx-auto ">
            <textarea name="khataData" id="khataData" rows="10" class="w-full px-3 py-2 border border-gray-300 shadow-lg shadow-gray-200 rounded-md focus:outline-none focus:border-blue-500" placeholder="Enter your Hisaab data here..."></textarea>
            <div class="flex justify-center">
                <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white px-8 py-2 rounded-md focus:outline-none">New Hisaab</button>
            </div>
        </form>
    </div>
</body>
</html>
